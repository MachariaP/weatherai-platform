# Copy to .env.local for local development. Never commit .env.local.
# Server-side only. Do not expose this URL to the browser.
# Local:
BACKEND_URL=http://localhost:8000
# Production (Vercel): set BACKEND_URL to the public HTTPS FastAPI origin.
